import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export default function KosPage() {
  return (
    <PagePlaceholder
      title="Cari Kos"
      description="Halaman listing kos akan dibuat menggunakan mock data lokal."
    />
  );
}